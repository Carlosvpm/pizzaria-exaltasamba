import { HeaderContainer } from "./header.styles";

export const Header = () => {
  const title: string = "ExaltaPizza";
  return (
    <HeaderContainer>
          <h1>{title}</h1>
          
    </HeaderContainer>
  );
};
